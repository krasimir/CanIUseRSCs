steps:
  # Build the container image
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      ["builds", "submit", "--tag", "gcr.io/wisepot/caniuserscs"]
  # Deploy container image to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "caniuserscs",
        "--image",
        "gcr.io/wisepot/caniuserscs",
        "--region",
        "europe-west1",
        "--platform",
        "managed",
        "--min-instances",
        "0",
        "--allow-unauthenticated"
      ]